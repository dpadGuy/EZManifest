<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="EZManifest.Views.Pages.DebugConsolePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="30" FontWeight="SemiBold" Text="Debug console" />

        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <ToggleSwitch
                x:Name="AutoScrollToggle"
                Grid.Column="1"
                VerticalAlignment="Center"
                IsOn="True"
                OffContent="Auto-scroll"
                OnContent="Auto-scroll" />
            <Button
                x:Name="CopyAllButton"
                Grid.Column="2"
                Content="Copy all"
                Click="CopyAllButton_Click" />
            <Button
                Grid.Column="3"
                Content="Clear"
                Click="ClearButton_Click" />
        </Grid>

        <Border
            Grid.Row="2"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="4">
            <ScrollViewer
                x:Name="LogScrollViewer"
                Padding="8,4"
                HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollMode="Disabled"
                VerticalScrollMode="Enabled"
                SizeChanged="LogScrollViewer_SizeChanged">
                <TextBlock
                    x:Name="LogTextBlock"
                    IsTextSelectionEnabled="True"
                    FontFamily="Consolas"
                    FontSize="12"
                    TextWrapping="WrapWholeWords"
                    IsHitTestVisible="True" />
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
